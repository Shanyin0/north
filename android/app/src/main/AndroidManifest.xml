<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 定位。两个都写：粗的（基站/WiFi，省电）和细的（GPS）。
         她在系统里亲手给，随时能收回；没给的话 GeoBridge 返回空，别的照跑 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- 存图到相册。安卓 10 起走 MediaStore 不需要这个，只给老系统留着 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <!-- 通话的时候立一根前台服务的桩子。没有它，她一切走安卓就把麦收回去
         （安卓 9 起不给后台 App 用麦），过一会儿连进程都可能被回收。
         安卓 14 起还要单独声明是「microphone」那一类，说了才留得住麦 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- 看她最近在用什么 App。这个权限系统不给自动授，
         必须她自己去「设置 → 应用 → 特殊权限 → 使用情况访问」里打开，随时能关 -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <!-- 安卓 11 起，别的 App 默认互相看不见。不问一句，连名字都读不到，
         列出来就全是 com.xxx 那种。这里只问「桌面上有图标的那些」——
         系统的后台服务、安装器、权限控制器都不在里面，正好也不用显示 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".PushReceiver" android:exported="false" />

        <receiver android:name=".BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- 通话中那根桩子。exported=false —— 只有梦匣自己叫得动它 -->
        <service
            android:name=".CallService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

    </application>
</manifest>
